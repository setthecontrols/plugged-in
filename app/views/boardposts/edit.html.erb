<div class="container">
  <h2 class="cyan-text text-darken-3">Edit This Posting</h2>
</div>


<%= form_for @post, html: {multipart:true} do |f|%>
  <%= f.text_area :title %>
  <%= f.text_area :content %>
  <div id="tag-form">
  </div>
  <%= link_to 'Add Tag', new_tag_path, :class => "addTag"%> <br>
  <%= f.file_field :image %>
  <%= submit_tag "Submit" %>
<%  end %>
